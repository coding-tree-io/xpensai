# Agent Instructions

## Verifications
Run these after every code change:

- Type check: `npm run lint`
- Build: `npm run build`
- TypeScript: `npx tsc --noEmit`

If a command cannot run (missing env vars or services), note the blocker and continue.
